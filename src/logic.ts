const formatNumber = (template: string, input: string) => {
  let count = 1;
  const separator = "    -    ";
  const placeholder = "xxxx    -    xxxx    -    xxxx    -    xxxx";
  
};

export default formatNumber